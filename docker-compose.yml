version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      # For Neon: replace with your actual Neon credentials
      - SPRING_DATASOURCE_URL=jdbc:postgresql://ep-fragrant-term-a1qix6kk-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
      - SPRING_DATASOURCE_USERNAME=neondb_owner
      - SPRING_DATASOURCE_PASSWORD=npg_Gjm6tloF3eSg
    volumes:
      - uploads:/app/uploads

volumes:
  uploads:
